'use client';

import { ensureNativeModulesAreInstalled } from 'expo-modules-core/src/ensureNativeModulesAreInstalled';
import type { SharedRef as SharedRefType } from 'expo-modules-core/src/ts-declarations/SharedRef';

ensureNativeModulesAreInstalled();

const SharedRef = globalThis.expo.SharedRef as typeof SharedRefType;

export default SharedRef;
